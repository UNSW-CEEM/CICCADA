# Notebook workflow

The Python modules in `src/ausgrid_analysis` contain the reusable pipeline logic.

Notebooks call those modules one stage at a time and expose validation outputs before the next stage.

